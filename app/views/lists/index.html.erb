<% @list = List.new unless @list %>

<% @lists.each do|list| %>
<%= list.id %>
<%= list.title %>
<% end %>

<%= form_for(@list) do |f| %>
<div>
  <%= f.label :title %>
  <%= f.text_field :title, size: 50 %>
</div>
<%= f.submit '作成' %>
<% end %>